{
  "name": "wacli-reader",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "wacli-reader": "bash -lc 'pnpm -s build >/dev/null && exec ./dist/wacli-reader \"$@\"' _",
    "build": "mkdir -p dist && CGO_CFLAGS=\"${CGO_CFLAGS:+$CGO_CFLAGS }-Wno-error=missing-braces\" go build -tags sqlite_fts5 -o dist/wacli-reader ./cmd/wacli",
    "start": "pnpm -s build && ./dist/wacli-reader",
    "test": "pnpm -s test:go && pnpm -s test:fts",
    "test:go": "go test ./...",
    "test:fts": "go test -tags sqlite_fts5 ./...",
    "lint": "go vet ./...",
    "format": "gofmt -w .",
    "format:check": "bash -lc 'out=$(gofmt -l .); if [ -n \"$out\" ]; then echo \"$out\"; exit 1; fi'",
    "generate:sqlc": "go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1 generate"
  }
}
